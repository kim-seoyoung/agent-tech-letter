"""Renderer layer — converts `RenderedIssue` to channel-specific output.

Per CR-0001 / EP0005: one component layer (`templates/components/`) drives
both the web page (`html_web`) and the email body (`html_email`).
"""
