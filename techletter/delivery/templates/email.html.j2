<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="color-scheme" content="light only">
<title>AI Agent Weekly — {{ issue_date_iso }}</title>
<style>
body { font-family: {{ font.family|safe }}; color: {{ colors.fg|safe }}; background: #f5f5f5; margin: 0; padding: 0; }
.kicker { font-family: {{ font.mono|safe }}; font-size: {{ font.size_kicker|safe }}; letter-spacing: 0.2em; text-transform: uppercase; color: {{ colors.accent|safe }}; margin: 0 0 10px 0; }
h1 { font-size: {{ font.size_h1|safe }}; font-weight: 800; color: {{ colors.fg|safe }}; margin: 0; letter-spacing: -0.03em; line-height: 1; }
h1 em { font-style: normal; background: linear-gradient(transparent 62%, {{ colors.accent_wash|safe }} 62%); padding: 0 2px; }
.rule { font-size: 1px; line-height: 3px; height: 3px; background: {{ colors.fg|safe }}; margin: 18px 0 12px 0; }
h2 { font-size: {{ font.size_h2|safe }}; color: {{ colors.fg|safe }}; margin: 0 0 12px 0; letter-spacing: -0.01em; }
.deep-dive h2 { font-size: {{ font.size_h2|safe }}; color: {{ colors.fg|safe }}; font-weight: 700; }
.dd-index { font-family: {{ font.mono|safe }}; font-size: {{ font.size_meta|safe }}; letter-spacing: 0.14em; text-transform: uppercase; color: {{ colors.accent|safe }}; margin: 0 0 12px 0; }
.tags { font-family: {{ font.mono|safe }}; font-size: {{ font.size_meta|safe }}; margin: 0 0 22px 0; }
.body { color: {{ colors.fg|safe }}; font-family: {{ font.serif|safe }}; font-size: {{ font.size_body|safe }}; line-height: 1.78; }
.body p { margin: 0 0 15px 0; }
.body h3, .body h4 { margin: 32px 0 10px 0; font-family: {{ font.family|safe }}; font-size: {{ font.size_h3|safe }}; font-weight: 600; color: {{ colors.fg|safe }}; line-height: 1.4; }
.body ul, .body ol { margin: 0 0 16px 0; padding-left: 24px; }
.body li { margin-bottom: 6px; }
.body blockquote { margin: 16px 0; padding: 4px 0 4px 16px; border-left: 3px solid {{ colors.border|safe }}; color: {{ colors.muted|safe }}; }
.date { color: {{ colors.muted|safe }}; margin: 0 0 40px 0; font-family: {{ font.mono|safe }}; font-size: {{ font.size_meta|safe }}; letter-spacing: 0.03em; }
.tag { background: {{ colors.tag_bg|safe }}; padding: 2px 8px; border-radius: 4px; font-size: 12px; }
.sources { font-family: {{ font.mono|safe }}; font-size: 12px; color: {{ colors.muted|safe }}; margin: 22px 0 0 0; padding-top: 14px; border-top: 1px dashed {{ colors.border|safe }}; }
a { color: {{ colors.accent|safe }}; text-decoration: none; }
code { font-family: {{ font.mono|safe }}; background: {{ colors.tag_bg|safe }}; padding: 1px 4px; border-radius: 3px; font-size: 0.92em; }
.qm { margin-bottom: 10px; color: {{ colors.fg|safe }}; font-size: {{ font.size_body|safe }}; }
.qm-section { margin-top: 64px; padding-top: 32px; border-top: 2px solid {{ colors.fg|safe }}; }
.qm-kicker { margin: 0 0 6px 0; font-family: {{ font.mono|safe }}; font-size: {{ font.size_kicker|safe }}; letter-spacing: 0.18em; text-transform: uppercase; color: {{ colors.accent|safe }}; }
</style>
</head>
<body style="margin:0; padding:0; background:#f5f5f5;">
<table role="presentation" width="100%" cellpadding="0" cellspacing="0" border="0">
<tr><td align="center" style="padding: 24px 0;">
<table role="presentation" width="680" cellpadding="0" cellspacing="0" border="0" style="background: {{ colors.bg|safe }}; max-width: {{ layout.max_width|safe }};">
<tr><td style="padding: {{ layout.padding|safe }};">
<p class="kicker">Weekly intelligence · 에이전트 생태계</p>
<h1>AI <em>Agent</em> Weekly</h1>
<div class="rule">&nbsp;</div>
<p class="date">{{ issue_date_iso }}</p>
{%- for dd in deep_dives %}
{%- set is_first = loop.first %}
{%- set dd_index = loop.index %}
{% include "components/deep_dive.html.j2" %}
{%- endfor %}
<div class="qm-section">
<p class="qm-kicker">Also this week</p>
<h2>Quick Mentions</h2>
<ul style="padding-left: 20px; margin: 0;">
{%- for qm in quick_mentions %}
{% include "components/quick_mention.html.j2" %}
{%- endfor %}
</ul>
</div>
</td></tr>
</table>
</td></tr>
</table>
</body>
</html>
